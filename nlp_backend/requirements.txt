# SignVani Production Dependencies
# Compatible with Python 3.12+

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Speech Recognition (Offline)
vosk==0.3.45

# Audio Processing
PyAudio==0.2.14
soundfile==0.12.1

# Numerical Computing (wheels available for Python 3.12)
numpy>=1.26.0
scipy>=1.11.0

# Natural Language Processing (Lightweight)
nltk>=3.8.1

# SQLite is built into Python standard library (no install needed)

# Note: PyAudio on Windows requires pre-built wheels
# Install using: pip install pipwin && pipwin install pyaudio
#
# On Linux/RPi4, install system dependencies first:
# sudo apt-get install portaudio19-dev python3-dev libasound2-dev
# then: pip install -r requirements.txt
